name: sct
description: SimBuCore Tools to run and report on flutter_gherkin feature tests
version: 1.0.0
# homepage: https://www.example.com
publish_to: 'none'

environment:
  sdk: ">=2.16.2 <3.0.0"

dev_dependencies:
  lints: ^1.0.0

dependencies:
  args: ^2.3.0
  # Cross project code to support flutter application development.
  simbucore:
    git: https://gitlab.com/simbu-mobile/simbucore.git    
  